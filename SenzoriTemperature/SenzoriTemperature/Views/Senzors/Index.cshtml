@model IEnumerable<SenzoriTemperature.Models.Senzor>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.lokacija)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vremeMerenja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.temperatura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.poslovniPartner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.datumServisa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.lokacija)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.vremeMerenja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.temperatura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.poslovniPartner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.datumServisa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
